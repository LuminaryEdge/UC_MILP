目标函数部分
1、只计算开机费用，不要关机费用 （已删除：在设定目标函数时候去掉了cd，并删除了跟cd有关的3条约束(14)(15)）
2、你的开机费用可能有问题，建议先不用分段函数，只计算热启动，就像你关机费用计算方法就行（已完成。开机费用之前写了好久，悲）
3、燃料费用你是如何分段的以及你的斜率计算方法具体是什么呀（用了一个pwlf(piece-wise linear function)的package(根据函数点，指定分段函数然后进行分段，可以保存每一段的斜率F和分割点）具体使用的是什么方法我也没仔细研究，差分进化bulabula）
机组约束部分
1、爬坡约束只要约束18就行，其他我们没有数据，先不用写 (已删除其他代码)
2、你的最小开关机约束可能有问题，它应该是一个紧约束，即对目标值有影响，但是你的加上去掉没变化，初步观察应该是你的range用法不对，注意一下range的范围，判断是range(N)还是range(N+1)（自查了好久没检查出来，可以讨论一下吗？因为存在range下标-1的问题，所以思考起来比较困难，是不是可以有更简单的下标表示方式？）
3、加上网络约束
系统约束部分
暂时没有问题

其他方面
1. 使用pandas读入数据时不知道为什么相对路径用不了了，只能用绝对路径，修改位置分别是266行和509行，麻烦学姐跑程序的时候注意一下